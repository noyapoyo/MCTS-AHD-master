- Preserve useful parent adjacency instead of rebuilding random permutations.
- Use distance_matrix only for bounded local edge choices; taskGA owns full tour scoring.
- Return result = (crossover_child, mutated_route).
